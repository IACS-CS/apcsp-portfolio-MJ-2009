import { renderMenu } from "../utils/renderMenu";
renderMenu

export function renderCitations() {
/* html */
  return `
    <h1>Citations</h1>
    ${renderMenu()}
    <p> initial template from my teacher as well as help with fixing the issues I had with buttons.</p>
    <P> Used Copilot with initial help with creating buttons, And further assistance with styling.</p>
    <P> later include pico.css I did not use any creative commons licenses I have all rights reserved.</p>

    `;
  
}